---
# ============================================================================
# CASE STUDY TEMPLATE
# ----------------------------------------------------------------------------
# Copy this file into /content/automation/ or /content/marketing/ and rename it.
# The filename becomes the URL:  my-new-build.mdx  ->  /automation/my-new-build
# Fill everything in, commit, done. No design work needed — ever.
#
# Files starting with "_" are never published, so this template stays private.
# ============================================================================

# The headline. Say what the system does, not what it is called.
title: ""

# Client name, or "Personal project", or a site like "quality-buy.online".
client: ""

# "automation" or "marketing" — must match the folder you put this file in.
lane: "automation"

# One sentence a non-technical client understands in 4 seconds.
summary: ""

# What you actually did, e.g. "Built and deployed" / "Strategy and build".
role: ""

# Display year, e.g. "2026".
year: ""

# ISO date. Used for sorting when two files share the same `order`.
date: "2026-01-01"

# Lower numbers sit higher on the lane page. 1 = top.
order: 1

# true = this case study's TOP metric appears in the home page proof strip.
featured: false

# OPTIONAL. Put the image at /public/case-studies/<this-filename>/cover.jpg
# and uncomment the line. Leave it out and a clean typographic card shows instead.
# cover: "/case-studies/my-new-build/cover.jpg"

# OPTIONAL. A Loom/YouTube EMBED url (not the share url). Leave out if none.
# video: "https://www.loom.com/embed/XXXXXXXX"

# What you built it with.
tools: []

# OPTIONAL. The first one shows on the lane card and in the proof strip,
# so put your strongest number first. Leave the list empty if you have none.
metrics: []
#  - label: "Response time"
#    value: "12h → 4 min"
#  - label: "Leads handled/mo"
#    value: "300+"
---

{/* Keep these four sections, in this order, in every case study.
    That consistency is the point — every write-up reads the same way. */}

## The problem

Two or three sentences, in the client's own language. What was going wrong,
who it was hurting, and what it was costing. No jargon.

## What I built

The system itself, and the tools it runs on. Be concrete: what triggers it,
what it touches, where the output lands.

## How it works, explained simply

The teaching section — this is the part that sets the whole site apart.
Explain the mechanism in plain sentences a non-technical person could repeat
back to a colleague. Use an analogy if it helps. Short sentences.

## The outcome

What changed. Numbers where you have them, honest qualifiers where you don't.
Finish with what the client can now do that they could not do before.
